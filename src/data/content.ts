import type { Content } from '../types';

export const courseContent: Content[] = [
  // =================================================================
  // Python Basics - ОБНОВЛЕНО И УЛУЧШЕНО
  // =================================================================
  {
    id: 'python-vars',
    title: 'Основы Python: Переменные и Типы Данных 📦',
    description: 'Самый первый шаг! Учимся хранить данные в "коробках"-переменных, знакомимся с их типами и общаемся с программой с помощью `print` и `input`.',
    category: 'Основы Python',
    tags: ['python', 'переменные', 'типы данных', 'print', 'input'],
    content: [
      `### Что такое переменная? 🤔
Представь, что у тебя есть несколько волшебных коробок. Ты можешь положить в них что угодно: яблоко, записку с текстом, число. Чтобы не запутаться, ты подписываешь каждую коробку. В программировании **переменные** — это и есть такие подписанные "коробки" для данных, которые хранятся в памяти компьютера.

В Python создать переменную очень просто: даешь ей имя, ставишь знак \`=\` и кладешь значение.

**Правила хорошего имени для переменной:**
- ✅ Имя должно говорить, что внутри (вместо \`x = "Петя"\` лучше \`player_name = "Петя"\`).
- ❌ Имя не может начинаться с цифры (\`1st_player\` - ошибка!).
- ❌ В имени не должно быть пробелов (вместо "player name" используй \`player_name\`).
- 🤫 Имена \`print\`, \`input\`, \`int\` и другие — "заняты" самим Python, их лучше не использовать.`,
      {
        language: 'python',
        code: `# Это комментарий. Python его не читает, он для нас, людей.

# Создаем переменную (коробку) с именем user_name 
# и кладем в нее текст "Алиса". Текст всегда пишется в кавычках "" или ''.
user_name = "Алиса"

# Создаем переменную user_age и кладем в нее целое число.
user_age = 14

# Создаем переменную с дробным числом (например, рост в метрах).
# Дробные числа пишутся через точку!
user_height = 1.65 

# А это переменная-переключатель. Может быть либо True (правда), либо False (ложь).
is_student = True

# Команда print() "заглядывает" в коробки и показывает, что там лежит.
# Мы используем f-строку, чтобы красиво всё вывести.
print(f"Имя: {user_name}")
print(f"Возраст: {user_age}")
print(f"Рост: {user_height} м")
print(f"Является учеником: {is_student}")`
      },
      `### Диалог с программой: команда \`input()\` 💬
Команда \`input()\` — это способ программы задать нам вопрос. Она останавливает свое выполнение и ждет, пока мы что-нибудь напишем в консоли и нажмем **Enter**.`,
      {
        language: 'python',
        code: `# Спрашиваем имя пользователя и ждем, пока он что-нибудь напишет.
# Ответ пользователя сохранится в переменную guest_name.
guest_name = input("Привет! Как тебя зовут? ")

# f-строка позволяет вставлять переменные прямо в текст.
print(f"Очень приятно, {guest_name}! Добро пожаловать в мир Python!")`
      },
      `### Превращение типов: Магия для калькулятора ✨
**Очень важный момент:** \`input()\` всегда возвращает **текст (str)**! Даже если ты ввел цифры. Нельзя сложить "текст 5" и "текст 10", получится "510" (склеивание текста), а не 15.

Чтобы делать математику, нам нужно превратить текст в числа с помощью команд:
- \`int()\` — превращает в **целое** число (integer).
- \`float()\` — превращает в **дробное** число (float).`,
      {
        language: 'python',
        code: `print("--- 덧셈을 위한 간단한 계산기 ---")

# Спрашиваем первое число. Оно будет текстом!
num1_str = input("Введите первое число: ")
# Превращаем текст в целое число
num1_int = int(num1_str)

# То же самое для второго числа, но можно сделать в одну строчку!
num2_int = int(input("Введите второе число: "))

# Теперь, когда у нас есть два числа, мы можем их сложить
result = num1_int + num2_int

print(f"Результат: {num1_int} + {num2_int} = {result}")`
      },
      `### ⭐ Попробуй сам!
Напиши программу-анкету. Она должна:
1. Спросить имя, возраст и город проживания.
2. Сохранить все ответы в переменные.
3. Вывести красивую итоговую анкету, например: "Итак, тебя зовут [Имя], тебе [Возраст] лет и ты из города [Город]. Здорово!"`
    ],
  },
  {
    id: 'python-if',
    title: 'Основы Python: Условия if-elif-else ⚔️',
    description: 'Учим программу принимать решения и выбирать путь, как рыцарь на распутье. Добавляем логику с `and`, `or` и `not`.',
    category: 'Основы Python',
    tags: ['python', 'ветвления', 'условия', 'if-else', 'логика'],
    content: [
`### Развилка на дороге
Представь, что программа — это рыцарь, который дошел до камня с надписью: "Налево пойдешь — коня потеряешь, направо пойдешь — жизнь найдешь". Конструкция \`if-elif-else\` — это и есть такой камень, который заставляет программу выбирать один путь в зависимости от условия.

- **\`if\` (Если):** Проверяет первое, главное условие. Если оно правдиво (\`True\`), выполняется код под ним.
- **\`elif\` (Иначе если):** Если \`if\` оказался ложным (\`False\`), проверяется это условие. \`elif\`'ов может быть сколько угодно.
- **\`else\` (Иначе):** Если вообще ни одно из условий выше не сработало, выполняется этот блок.

**Важно:** код, который должен выполниться, пишется с **отступом** (обычно 4 пробела). Так Python понимает, что эта команда относится к условию. Если отступа не будет — будет ошибка \`IndentationError\`!`,
{
  language: 'python',
  code: `age = int(input("Сколько вам лет? "))

if age >= 18:
    print("✅ Доступ разрешен. Добро пожаловать!")
elif age >= 14:
    print("🤔 Доступ разрешен в сопровождении взрослых.")
else:
    print("❌ Доступ запрещен. Вам еще рано.")`
},
`### Логические союзы: "И", "ИЛИ", "НЕ"
Иногда нужно проверить сразу несколько условий.
- \`and\` (И): Условие правдиво, только если **ОБЕ** его части правдивы.
- \`or\` (ИЛИ): Условие правдиво, если **ХОТЯ БЫ ОДНА** из его частей правдива.
- \`not\` (НЕ): Переворачивает условие наоборот. \`not True\` становится \`False\`.`,
{
  language: 'python',
  code: `weather = "дождь"
mood = "хорошее"
homework_done = False

# Проверяем: погода "солнечная" И настроение "хорошее"? Нет.
if weather == "солнечно" and mood == "хорошее":
    print("Идем гулять в парк! ☀️")
# Окей, тогда проверяем: погода "дождь" ИЛИ настроение "хорошее"? Да, настроение хорошее.
elif weather == "дождь" or mood == "хорошее":
    print("Можно посидеть дома и посмотреть фильм. 🍿")
# Если домашка НЕ сделана
elif not homework_done:
    print("Сначала нужно сделать домашку! 📚")
# Если бы все условия выше были ложными
else:
    print("Лучше остаться дома и лечь спать. 😴")`
},
`### Вложенные проверки 🎁
Иногда после одной проверки нужно сделать еще одну. Это как открыть сундук, а внутри него еще один, поменьше.`,
{
  language: 'python',
  code: `age = 15
has_ticket = True

if age >= 12:
    print("Проверка по возрасту пройдена.")
    # Это вложенная проверка. Она сработает, только если первая была успешной.
    # Обрати внимание на двойной отступ!
    if has_ticket: # Можно писать if has_ticket вместо if has_ticket == True
        print("Проверка билета пройдена. Проходите в кино! 🎟️")
    else:
        print("Упс, у вас нет билета! 😠")
else:
    print("Вы слишком малы для этого фильма. 👶")`
},
`### ⭐ Попробуй сам!
Напиши программу-советчик. Она спрашивает температуру на улице (числом).
- Если температура больше 20, советует надеть футболку.
- Если температура между 10 и 20 (включительно), советует надеть свитер.
- Если температура меньше 10, советует надеть куртку.`
]
  },
  {
    id: 'python-lists',
    title: 'Основы Python: Списки (массивы) 🎒',
    description: 'Учимся хранить много данных в одном месте. Списки — это как рюкзак для переменных, в который можно класть и доставать вещи.',
    category: 'Основы Python',
    tags: ['python', 'списки', 'массивы', 'методы', 'срезы', 'индексы'],
    content: [
`### Что такое список?
Представь, что у тебя есть рюкзак. Ты можешь класть в него разные вещи: книгу, яблоко, телефон. **Список (list)** в Python — это такой же "рюкзак" для данных. Это упорядоченная коллекция, где у каждого элемента есть свой **номер (индекс)**.

**Самое главное:** нумерация в программировании всегда начинается с **НУЛЯ**! Первый элемент имеет индекс 0, второй — 1, и так далее.`,
{
  language: 'python',
  code: `# Создаем список покупок
#             индекс 0      1         2        3         4
shopping_list = ["Хлеб", "Молоко", "Яйца", "Сыр", "Колбаса"]

# Достаем что-то одно по его номеру (индексу)
print(f"Первый в списке: {shopping_list[0]}")  # Хлеб
print(f"Третий в списке: {shopping_list[2]}")  # Яйца

# Можно поменять элемент по его индексу
print(f"Старый список: {shopping_list}")
shopping_list[1] = "Кефир" # Вместо молока теперь кефир
print(f"Новый список: {shopping_list}")`
},
`### Полезные команды для списков
Списки очень мощные, у них много встроенных "умений" (методов).

- \`.append(элемент)\` — добавляет новый элемент в **конец** списка.
- \`.pop(индекс)\` — удаляет элемент по индексу и возвращает его. Если индекс не указать, удалит последний.
- \`.remove(значение)\` — удаляет первый найденный элемент с таким значением.
- \`len(список)\` — узнает, сколько всего элементов в списке (длину).
- \`элемент in список\` — проверяет, есть ли такой элемент в списке (возвращает \`True\` или \`False\`).`,
{
    language: 'python',
    code: `# Наш инвентарь в игре
inventory = ["Меч", "Щит", "Хлеб"]
print(f"Инвентарь: {inventory}")
print(f"Предметов в инвентаре: {len(inventory)}") # 3

# Мы нашли зелье! Добавляем его в рюкзак.
inventory.append("Зелье")
print(f"Нашли зелье! Инвентарь: {inventory}")

# Мы съели хлеб. Удалим его.
inventory.remove("Хлеб")
print(f"Съели хлеб. Инвентарь: {inventory}")

# Проверяем, есть ли у нас меч?
if "Меч" in inventory:
    print("Ура, у нас есть меч для защиты!")`
},
`### Срезы: берем сразу несколько предметов 🍰
Срезы позволяют получить не один элемент, а целый кусок списка. Используется синтаксис \`[старт:конец]\`.
- \`старт\`: номер элемента, с которого начинаем (включая его).
- \`конец\`: номер элемента, на котором заканчиваем (НЕ включая его!).`,
{
  language: 'python',
  code: `//                          0        1        2       3        4
inventory = ["Меч", "Щит", "Зелье", "Хлеб", "Карта"]

// Берем элементы с 1-го по 3-й (то есть 1-й и 2-й)
first_items = inventory[1:3]
print(f"Первые два предмета: {first_items}") // ['Щит', 'Зелье']

// Если не указать начало, срез пойдет с самого начала
first_two = inventory[:2]
print(f"Первые два: {first_two}") // ['Меч', 'Щит']

// Если не указать конец, срез пойдет до самого конца
all_but_first = inventory[1:]
print(f"Все, кроме первого: {all_but_first}") // ['Щит', 'Зелье', 'Хлеб', 'Карта']`
},
`### ⭐ Попробуй сам!
Создай список дел на сегодня.
1. Добавь в него 3-4 дела.
2. Потом добавь еще одно важное дело в конец.
3. Потом "выполни" (удали) самое первое дело.
4. В конце выведи на экран надпись "Осталось сделать:" и сам список оставшихся дел.`
]
  },
  {
    id: 'python-loops',
    title: 'Основы Python: Цикл for 🔁',
    description: 'Заставляем компьютер делать однотипную работу за нас. Изучаем, как "пробежаться" по списку и использовать `range`.',
    category: 'Основы Python',
    tags: ['python', 'циклы', 'for', 'range', 'итерация'],
    content: [
`Циклы нужны, чтобы повторять один и тот же код много раз, не копируя его.

### Цикл \`for\` для списков
Цикл \`for\` идеально подходит, чтобы "пробежаться" по каждому элементу в списке и что-то с ним сделать.`,
{
  language: 'python',
  code: `friends = ["Аня", "Боря", "Вика", "Глеб"]

# "Для каждого имени 'friend' в списке 'friends'..."
print("Список моих друзей:")
for friend in friends:
  # Этот код с отступом повторится для каждого элемента списка
  print(f"- {friend}")

print("Пора в школу!")`
},
`### Цикл \`for\` и волшебная команда \`range()\`
Часто нужно повторить что-то определенное количество раз. Для этого идеально подходит \`range()\`.
- \`range(5)\` создает последовательность чисел от 0 до 4 (всего 5 чисел).
- \`range(1, 6)\` создает последовательность от 1 до 5.
- \`range(0, 10, 2)\` создает последовательность от 0 до 9, но с шагом 2 (0, 2, 4, 6, 8).`,
{
  language: 'python',
  code: `// "Для каждого числа i в последовательности от 0 до 4..."
for i in range(5):
  print(f"Привет, мир! (Сообщение номер {i})")

print("---")

// Напечатает числа от 1 до 5
print("Считаем до пяти:")
for number in range(1, 6):
    print(number)

print("---")

# Напечатаем только четные числа до 10
print("Четные числа:")
for i in range(0, 11, 2):
    print(i)`
},
`### Вложенные циклы: цикл в цикле 🤯
Это как часовая и минутная стрелки. Пока часовая стрелка сделает один шаг, минутная должна пробежать целый круг.
Представь, что у тебя есть 5 коробок, и в каждой из них по 3 мяча. Чтобы пересчитать все мячи, ты берешь первую коробку и считаешь в ней 3 мяча, потом вторую и в ней 3, и так далее. Это и есть вложенный цикл.

Давайте напечатаем таблицу умножения!`,
{
  language: 'python',
  code: `# Внешний цикл для чисел от 1 до 9
for i in range(1, 10):
    # Внутренний цикл тоже для чисел от 1 до 9
    for j in range(1, 10):
        # end="\\t" - это чтобы print не переносил строку, а ставил красивый отступ (табуляцию)
        # Так числа выстраиваются в ровные колонки.
        print(i * j, end="\\t")
    # Когда внутренний цикл закончил свою работу (для одного числа i),
    # мы делаем пустой print(), чтобы перейти на новую строку для следующего ряда таблицы.
    print("")`
},
]
  },
  // =================================================================
  // НОВЫЙ КУРС!
  // =================================================================
  {
    id: 'python-while-break',
    title: 'Основы Python: Цикл while и управление 🚦',
    description: 'Изучаем цикл, который работает "пока условие истинно". Учимся управлять циклами с помощью `break` и `continue`.',
    category: 'Основы Python',
    tags: ['python', 'циклы', 'while', 'break', 'continue', 'бесконечный цикл'],
    content: [
`### Что такое цикл \`while\`?
Если цикл \`for\` похож на поезд, который едет от станции А до станции Б, то цикл \`while\` (пока) — это охранник, который стоит на посту, **ПОКА** горит зеленая лампочка. Как только лампочка погаснет (условие станет \`False\`), он уходит.

Цикл \`while\` идеально подходит, когда мы не знаем точно, сколько раз нужно повторить действие.`,
{
  language: 'python',
  code: `health = 100
print(f"Начинаем бой! Ваше здоровье: {health}")

# Цикл будет продолжаться, ПОКА здоровье больше 0.
while health > 0:
    print(f"Вы получаете удар! Ваше текущее здоровье: {health}")
    # Важно! Внутри цикла while нужно обязательно менять условие.
    # Иначе цикл станет бесконечным!
    health -= 10 # Уменьшаем здоровье на 10 (то же самое что health = health - 10)

print(f"Бой окончен! Ваше здоровье: {health}. Вы проиграли.")`
},
`### Бесконечный цикл и команда \`break\`
Иногда удобно сделать "вечный" цикл с помощью \`while True\`, а выходить из него по специальной команде.
- \`break\` (сломать) — немедленно **прерывает** и выходит из цикла.

Это очень популярный прием для создания меню или чатов.`,
{
  language: 'python',
  code: `print("Добро пожаловать в таверну 'Три Гнома'! Введите 'выход', чтобы уйти.")

while True: # Этот цикл будет работать вечно...
    command = input("> Введите команду: ")
    print(f"Вы ввели: '{command}'")
    
    if command == "выход":
        print("Вы вышли из таверны. До свидания!")
        break # ...но команда break его остановит!

print("Программа завершена.")`
},
`### Команда \`continue\`
- \`continue\` (продолжить) — не выходит из цикла, а **пропускает** всё, что идет после, и немедленно переходит к следующему шагу (итерации).`,
{
  language: 'python',
  code: `# Мы хотим напечатать все числа от 0 до 10, кроме 5 и 7
print("Печатаем числа с пропусками:")
for i in range(11):
    if i == 5 or i == 7:
        continue # Пропускаем числа 5 и 7, и идем к следующему i
    
    print(i)`
},
`### ⭐ Попробуй сам!
Используя \`while True\` и \`break\`, напиши программу-копилку.
1. Программа в цикле спрашивает: "Сколько положить в копилку?".
2. Пользователь вводит число, и оно добавляется к общей сумме.
3. После каждого добавления программа пишет: "В копилке уже [сумма] монет."
4. Если пользователь вводит не число, а слово "стоп", программа должна остановиться.`
]
  },
  {
    id: 'python-dicts',
    title: 'Основы Python: Словари 📇',
    description: 'Храним данные парами "ключ-значение". Словари — как карточки для персонажа в игре или анкета.',
    category: 'Основы Python',
    tags: ['python', 'словари', 'структуры данных', 'json', 'ключ-значение'],
    content: [
`### Что такое словарь?
Если список (рюкзак) хранит элементы по номерам (0, 1, 2...), то **словарь (dict)** — это как анкета или карточка персонажа. У каждой характеристики есть свое уникальное название (**ключ**) и ее **значение**. Например: \`"здоровье": 100\`, \`"мана": 50\`.

Ключи — это всегда текст (или числа), а значения могут быть чем угодно: текстом, числом, другим списком или даже другим словарем!`,
{
  language: 'python',
  code: `# Создаем словарь, описывающий персонажа
character = {
    "имя": "Гэндальф",
    "класс": "Волшебник",
    "здоровье": 80,
    "мана": 150,
    "в_команде": True,
    "инвентарь": ["Посох", "Трубка", "Книга заклинаний"]
}

# Чтобы узнать ману, мы обращаемся к ключу "мана"
print(f"Мана персонажа: {character['мана']}")

# Можно легко добавить новую характеристику
character["оружие"] = "Посох 'Пламя Анора'"
print(f"Персонаж нашел новое оружие!")

# Или изменить существующую
character["здоровье"] = 95
print(f"Персонаж выпил зелье. Здоровье теперь: {character['здоровье']}")

# Можно узнать, какой предмет первый в инвентаре
print(f"Первый предмет в инвентаре: {character['инвентарь'][0]}")`
},
`### Как безопасно работать со словарями
Если ты попробуешь достать значение по ключу, которого нет, программа выдаст ошибку \`KeyError\`. Этого можно избежать.
- \`ключ in словарь\` — проверит, есть ли такой ключ.
- \`.get(ключ, значение_по_умолчанию)\` — попытается достать значение. Если ключа нет, вернет не ошибку, а значение по умолчанию (например, \`None\` или то, что ты укажешь).`,
{
  language: 'python',
  code: `hero = {"name": "Арагорн", "level": 10}

# Проверяем, есть ли у героя ключ "сила"
if "сила" in hero:
    print(f"Сила героя: {hero['сила']}")
else:
    print("У героя еще нет характеристики 'сила'")

# Безопасный способ получить значение с помощью .get()
# Если ключа 'weapon' нет, вернется строка 'Руки'
weapon = hero.get("weapon", "Руки")
print(f"Оружие героя: {weapon}")`
},
`### Как перебрать все в словаре?
Можно использовать цикл \`for\`, чтобы посмотреть все ключи и значения.`,
{
    language: 'python',
    code: `student_grades = {"Математика": 5, "История": 4, "Химия": 5, "Физкультура": 3}

print("--- Оценки ученика ---")
# Метод .items() позволяет в цикле сразу получать и ключ, и значение
for subject, grade in student_grades.items():
    print(f"Предмет: {subject}, Оценка: {grade}")

# Можно получить только все ключи или только все значения
print(f"\\nВсе предметы: {student_grades.keys()}")
print(f"Все оценки: {student_grades.values()}")`
}
]
  },
  {
    id: 'python-functions',
    title: 'Основы Python: Функции (Рецепты) 📜',
    description: 'Создаем "рецепты" для компьютера с несколькими ингредиентами, чтобы сделать код чище, удобнее и не повторяться.',
    category: 'Основы Python',
    tags: ['python', 'функции', 'def', 'return', 'аргументы', 'DRY'],
    content: [
`### Зачем нужны функции?
**Функция** — это именованный "рецепт" (блок кода), который можно "приготовить" (вызвать) в любой момент по его имени. Это помогает не повторять один и тот же код много раз. Если ты видишь, что копируешь и вставляешь один и тот же кусок кода — это знак, что пора создать функцию! Этот принцип называется **DRY (Don't Repeat Yourself - Не повторяйся)**.

- \`def\` (от define - определить) — ключевое слово для создания функции.
- \`return\` (вернуть) — команда, которая отдает результат работы функции "наружу".`,
{
  language: 'python',
  code: `# Создаем функцию (рецепт) для расчета площади прямоугольника
# У нее два обязательных "ингредиента" (аргумента): length и width
def calculate_area(length, width):
  print(f"Считаю площадь для прямоугольника {length}x{width}...")
  area = length * width
  return area # Отдаем результат

# "Готовим" (вызываем) наш рецепт для торта размером 10 на 5
# Результат, который вернула функция, мы сохраняем в переменную
cake_area = calculate_area(10, 5)
print(f"Площадь торта: {cake_area} см²")

print("---")

# Можем использовать тот же рецепт для другого "торта"
table_area = calculate_area(120, 80)
print(f"Площадь стола: {table_area} см²")`
},
`### Ингредиенты по умолчанию
Иногда у рецепта есть ингредиент, который используется почти всегда. Например, соль. Мы можем указать его "по умолчанию". Если мы не передадим новую порцию соли, будет использоваться стандартная.`,
{
    language: 'python',
    code: `# Создаем функцию приветствия
# У нее есть обязательный ингредиент name
# и необязательный greeting, у которого значение по умолчанию "Привет"
def greet(name, greeting="Привет"):
  print(f"{greeting}, {name}!")

# Вызываем с одним ингредиентом, второй будет по умолчанию
greet("Вася") # Выведет: Привет, Вася!

# А тут мы передаем свое значение для greeting
greet("Маша", "Добрый вечер") # Выведет: Добрый вечер, Маша!

# Можно указывать аргументы по имени, тогда порядок не важен
greet(greeting="Хай", name="Олег") # Выведет: Хай, Олег!`
},
`### ⭐ Попробуй сам!
Напиши функцию \`is_even(number)\`.
1.  Она должна принимать одно число (\`number\`).
2.  Внутри функции она должна проверять, делится ли число на 2 без остатка. (Подсказка: оператор \`%\` находит остаток от деления. \`10 % 3\` будет \`1\`).
3.  Если число четное, функция должна возвращать \`True\`. Если нечетное — \`False\`.
4.  Вызови эту функцию несколько раз с разными числами и выведи результат на экран.`
]
  },
  // =================================================================
  // НОВЫЙ КУРС!
  // =================================================================
  {
    id: 'python-oop-intro',
    title: 'Python для продвинутых: Классы (Чертежи) 📐',
    description: 'Делаем шаг к профессиональному коду! Изучаем классы и объекты — способ создавать свои собственные типы данных, например, "Персонажа" для игры.',
    category: 'Python для продвинутых',
    tags: ['python', 'ооп', 'классы', 'объекты', 'class', '__init__', 'методы'],
    content: [
`### Зачем нужны классы?
Представь, что мы делаем игру. У нас будет много персонажей, и у каждого будет имя, здоровье, мана... Мы можем хранить их в словарях, но это не очень удобно. Было бы круто создать свой собственный тип данных, который бы назывался \`Character\`.

**Класс** — это **чертеж** или **формочка для печенья**. Он описывает, какими свойствами (переменными) и умениями (функциями) будет обладать будущий объект.
**Объект** — это **конкретный экземпляр**, созданный по этому чертежу. Например, по чертежу "Машина" можно создать объекты "Красный Феррари" и "Синий Жигули".`,
{
  language: 'python',
  code: `# Создаем чертеж (КЛАСС) для наших персонажей
class Character:
    # __init__ - это специальный "конструктор". Он вызывается, когда мы создаем НОВЫЙ объект.
    # Он устанавливает начальные характеристики.
    # self - это ссылка на сам объект (на конкретного гнома или эльфа).
    def __init__(self, name, race, health=100):
        self.name = name
        self.race = race
        self.health = health
        print(f"Создан персонаж: {self.name}, раса: {self.race}")

    # Это МЕТОД - функция внутри класса. Она описывает, что объект УМЕЕТ делать.
    def take_damage(self, damage):
        self.health -= damage
        print(f"{self.name} получает {damage} урона! Осталось здоровья: {self.health}")

    def show_info(self):
        print(f"--- ИНФО ---")
        print(f"Имя: {self.name}")
        print(f"Раса: {self.race}")
        print(f"Здоровье: {self.health}")
        print(f"--------------")


# Создаем ОБЪЕКТЫ (экземпляры) по нашему чертежу
# Теперь у нас есть два независимых персонажа!
gimli = Character("Гимли", "Гном", 150) # У гномов побольше здоровья
legolas = Character("Леголас", "Эльф") # У эльфа здоровье по умолчанию 100

# Давайте посмотрим информацию о них
gimli.show_info()
legolas.show_info()

print("\\n--- НАЧАЛАСЬ ДРАКА! ---")
# Используем их "умения" (методы)
legolas.take_damage(20)
gimli.take_damage(10)

legolas.show_info()
`
},
`### Наследование: Улучшаем чертежи
Представь, что мы хотим создать чертеж для "Мага", который будет похож на "Персонажа", но с дополнительной характеристикой — маной. Мы можем не копировать весь код, а **унаследовать** его!

**Наследование** позволяет создать новый класс (потомок) на основе существующего (родитель), добавив или изменив его функционал.`,
{
    language: 'python',
    code: `# Родительский класс (такой же, как в прошлом примере)
class Character:
    def __init__(self, name, race, health=100):
        self.name = name
        self.race = race
        self.health = health

    def show_info(self):
        print(f"Имя: {self.name}, Раса: {self.race}, Здоровье: {self.health}")

# Класс-потомок. В скобках указываем, от кого он наследуется.
class Mage(Character):
    # Мы создаем новый конструктор, т.к. хотим добавить ману
    def __init__(self, name, race, health=80, mana=150):
        # super().__init__() - вызывает конструктор родительского класса.
        # Это нужно, чтобы не писать заново self.name = name и т.д.
        super().__init__(name, race, health)
        self.mana = mana # А это наша новая, уникальная характеристика

    # Добавляем новый метод, которого нет у обычного Character
    def cast_spell(self):
        if self.mana >= 20:
            self.mana -= 20
            print(f"{self.name} кастует фаербол! Осталось маны: {self.mana}")
        else:
            print(f"У {self.name} не хватает маны!")
    
    # Мы можем ПЕРЕОПРЕДЕЛИТЬ родительский метод, чтобы он работал по-другому
    def show_info(self):
        # Сначала вызовем оригинальный метод, чтобы показать имя, расу, здоровье
        super().show_info()
        # А потом добавим вывод нашей новой информации
        print(f"Мана: {self.mana}")


# Создаем обычного воина
boromir = Character("Боромир", "Человек", 120)

# А это уже маг, созданный по новому чертежу
gandalf = Mage("Гэндальф", "Человек")

boromir.show_info()
print("---")
gandalf.show_info()
print("---")

gandalf.cast_spell()
gandalf.cast_spell()`
}
]
  },
  // =================================================================
  // НОВЫЙ МИНИ-ПРОЕКТ!
  // =================================================================
  {
    id: 'python-project-guess-number',
    title: 'Мини-проект: Игра "Угадай число" 🎲',
    description: 'Пишем первую настоящую игру! Компьютер загадывает число, а мы пытаемся его угадать. Применяем переменные, циклы и условия.',
    category: 'Python Проекты',
    tags: ['python', 'проект', 'игра', 'random', 'while', 'if-else'],
    content: [
`### План игры
Давай соберем все, что мы выучили, в один проект! Наша игра будет работать так:
1.  Компьютер загадывает случайное число от 1 до 100.
2.  Он говорит нам: "Я загадал число, попробуй угадать!".
3.  Мы вводим свой вариант в бесконечном цикле \`while True\`.
4.  Компьютер сравнивает наше число со своим и дает подсказку: "Слишком много", "Слишком мало" или "Ты угадал!".
5.  Если мы угадали, игра заканчивается.

### Шаг 1: Как заставить компьютер загадать случайное число?
В Python есть встроенная "библиотека" (набор готовых инструментов) под названием \`random\`. В ней есть функция \`randint(a, b)\`, которая возвращает случайное целое число между \`a\` и \`b\`.`,
{
    language: 'python',
    code: `# Чтобы использовать инструменты из библиотеки, ее нужно "импортировать"
import random

# Загадываем случайное число от 1 до 100 и сохраняем его
secret_number = random.randint(1, 100)

print("Я загадал число от 1 до 100. Попробуй угадать!")
# Раскомментируй следующую строку, если хочешь сжульничать и посмотреть ответ :)
# print(f"(Подсказка для разработчика: это число {secret_number})")

while True:
    # Спрашиваем число у пользователя и сразу превращаем его в int
    guess = int(input("Твой вариант: "))

    # Сравниваем числа
    if guess < secret_number:
        print("Слишком мало! Попробуй число побольше.")
    elif guess > secret_number:
        print("Слишком много! Попробуй число поменьше.")
    else:
        print(f"🎉 Поздравляю! Ты угадал! Это было число {secret_number}.")
        break # Выходим из цикла, т.к. игра окончена

print("Спасибо за игру!")`
},
`### ⭐ Попробуй улучшить игру!
Вот несколько идей, как сделать игру еще круче:
1.  **Счетчик попыток:** Создай переменную-счетчик. Прибавляй к ней 1 после каждой попытки. Когда игрок угадает, выведи "Ты угадал за [N] попыток!".
2.  **Ограничение попыток:** Дай игроку всего, например, 7 попыток. Если он не угадал, игра заканчивается поражением. (Подсказка: можно использовать \`if\` внутри цикла \`while\`, чтобы проверить счетчик и сделать \`break\`).
3.  **Проверка на жульничество:** Что будет, если пользователь введет число больше 100 или меньше 1? Добавь проверку, которая будет ругать его за это.`
]
  },
  // =================================================================
  // GigaChat & LangChain - ОБНОВЛЕНО И УЛУЧШЕНО
  // =================================================================
  {
    id: 'gigachat-simple',
    title: 'GigaChat: Наш первый разговор с ИИ 🤖',
    description: 'Учимся отправлять запросы в GigaChat и вести с ним простой диалог, который он будет помнить. Знакомимся с API-ключами.',
    category: 'LangChain & GigaChat',
    tags: ['gigachat', 'langchain', 'чат', 'сообщения', 'api'],
    content: [
`### Кто такой GigaChat и что такое API-ключ?
Представь, что GigaChat — это очень умный джинн, который живет в лампе. Чтобы его вызвать, тебе нужен специальный **секретный ключ (API-ключ)**. Этот ключ доказывает, что именно тебе разрешено с ним разговаривать. Ключ — это как пароль, его никому нельзя показывать!

**Аналогия:** GigaChat — это платный аттракцион в парке развлечений. API-ключ — это твой билет на этот аттракцион.

Сначала давайте просто зададим ему один вопрос.`,
{
  language: 'python',
  code: `# Сначала нужно установить библиотеку: pip install langchain-gigachat
from langchain_gigachat import GigaChat

# "Призываем" нашего джинна. 
# В credentials нужно вставить твой секретный ключ.
# verify_ssl_certs=False - техническая деталь, чтобы не ругался на сертификаты.
gigachat = GigaChat(credentials="ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_СЮДА", verify_ssl_certs=False)

print("Задаю вопрос джинну...")
# Спрашиваем джинна (вызываем его) и передаем наш вопрос
response = gigachat.invoke("Какой самый большой океан на Земле?")

# В ответе много всего, но сам текст ответа лежит в поле .content
print("Ответ джинна:")
print(response.content)`
},
`### Как сделать, чтобы джинн не забывал?
Проблема в том, что джинн очень забывчивый. Он отвечает на вопрос и тут же забывает, о чем вы говорили. Чтобы он помнил разговор, мы должны перед каждым новым вопросом показывать ему всю предыдущую переписку.

Мы будем хранить историю диалога в обычном списке. Для этого есть специальные типы сообщений:
- \`SystemMessage\`: Роль, которую мы даем ИИ ("Ты - пират").
- \`HumanMessage\`: Сообщение от нас, людей.
- \`AIMessage\`: Ответ от ИИ (джинна).`,
{
  language: 'python',
  code: `from langchain_gigachat import GigaChat
from langchain_core.messages import HumanMessage, SystemMessage, AIMessage

gigachat = GigaChat(credentials="ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_СЮДА", verify_ssl_certs=False)

# Это наша "папирусная лента", где мы записываем весь диалог
message_history = []

# Мы можем дать джинну роль. Он будет ее придерживаться.
# Это сообщение мы добавляем на ленту первым.
system_prompt = SystemMessage(content="Ты - дружелюбный пират, который ищет сокровища. Говори как пират.")
message_history.append(system_prompt)

print("Начат диалог с пиратом! Введи 'стоп', чтобы уйти.")

while True:
    user_input = input("Вы: ")
    if user_input.lower() == "стоп":
        print("Йо-хо-хо! До скорой встречи!")
        break

    # Записываем наш вопрос на ленту
    message_history.append(HumanMessage(content=user_input))
    
    # Показываем джинну ВСЮ ленту и получаем ответ
    ai_response = gigachat.invoke(message_history)
    
    # Записываем ответ джинна на ленту, чтобы он его не забыл для следующего вопроса
    message_history.append(ai_response)
    
    # Печатаем ответ
    print(f"Пират: {ai_response.content}")`
}
]
  },
  {
    id: 'gigachat-structured',
    title: 'GigaChat: Заставляем ИИ отвечать по анкете 📝',
    description: 'Учим GigaChat возвращать данные не просто текстом, а в строгом формате, который поймет компьютер, с помощью Pydantic.',
    category: 'LangChain & GigaChat',
    tags: ['gigachat', 'langchain', 'json', 'pydantic', 'структура'],
    content: [
`Обычно джинн-GigaChat отвечает нам просто текстом. Но что если нам нужно, чтобы он не просто рассказал о персонаже, а **заполнил анкету**? Например, чтобы мы могли легко сохранить его ответ в базу данных или использовать в программе.

### Зачем это нужно?
Если спросить: "Придумай персонажа-гнома по имени Торин, ему 150 лет, он любит пиво", джинн может ответить как угодно:
- "Вот гном: Торин, 150 лет. Любимый напиток - пиво."
- "Я придумал гнома! Его зовут Торин, возраст - 150 лет, и он обожает пиво."
Такой текст компьютеру сложно "разобрать". Но если мы заставим джинна заполнить анкету, мы получим четкий, предсказуемый ответ: \`имя="Торин", возраст=150, хобби="пить пиво"\`.

### Как создать "анкету" с помощью Pydantic?
Для создания анкет мы будем использовать помощника — библиотеку \`Pydantic\`. Мы создадим **класс** (чертеж нашей анкеты) и перечислим в нем все поля, которые джинн должен заполнить. Описание поля (\`description\`) — это подсказка для ИИ, чтобы он понял, что именно от него хотят.`,
{
  language: 'python',
  code: `# pip install pydantic
from langchain_gigachat import GigaChat
from langchain_core.pydantic_v1 import BaseModel, Field

# --- 1. Создаем чертеж нашей анкеты (класс Pydantic) ---
# Мы создаем класс "Character", который будет нашей анкетой.
# BaseModel - это волшебное слово от Pydantic, которое превращает класс в анкету.
class Character(BaseModel):
    # Описываем поля анкеты: имя поля, тип данных и описание-подсказку для джинна.
    name: str = Field(description="Имя персонажа")
    age: int = Field(description="Возраст персонажа")
    race: str = Field(description="Раса персонажа, например, Человек, Эльф или Гном")
    inventory: list[str] = Field(description="Список из 3 предметов в инвентаре персонажа")

# --- 2. "Призываем" джинна ---
gigachat = GigaChat(credentials="ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_СЮДА", verify_ssl_certs=False)

# --- 3. Связываем джинна с нашей анкетой ---
# .with_structured_output(Character) - это команда "Джинн, отныне отвечай, заполняя анкету Character!"
structured_gigachat = gigachat.with_structured_output(Character)

# --- 4. Делаем запрос ---
prompt = "Придумай мне персонажа. Это должен быть эльф-лучник по имени Леголас, которому 500 лет. В его рюкзаке должны быть лук, стрелы и лембас (эльфийский хлеб)."
print("Прошу джинна заполнить анкету...")
response = structured_gigachat.invoke(prompt)

# --- 5. Получаем заполненную анкету ---
print("\\nАнкета заполнена! Вот результат:")
# response - это теперь не просто текст, а объект нашего класса Character!
print(response) 
# Вывод будет примерно таким:
# name='Леголас' age=500 race='Эльф' inventory=['Лук', 'Стрелы', 'Лембас']

# Мы можем легко получить доступ к каждому полю
print("\\n--- Данные по отдельности ---")
print(f"Имя: {response.name}")
print(f"Возраст: {response.age}")
print(f"Раса: {response.race}")
print(f"Инвентарь: {response.inventory}")`
}
]
  },
  {
    id: 'langgraph-tools-memory',
    title: 'LangGraph: Даем ИИ инструменты и память 🛠️🧠',
    description: 'Создаем продвинутого агента, который может использовать наши собственные Python-функции (инструменты) и помнить весь диалог.',
    category: 'LangChain & GigaChat',
    tags: ['langgraph', 'gigachat', 'инструменты', 'память', 'агент'],
    content: [
`Обычный джинн-GigaChat может только болтать. Но мы можем научить его **фокусам**! Эти фокусы называются **"инструменты" (tools)**. Инструмент — это обычная функция на Python, которую джинн может сам решить вызвать, чтобы что-то сделать: посмотреть погоду, поискать товар в нашем "магазине", посчитать что-то на калькуляторе.

### Как научить джинна "фокусу"?
1.  **Пишем функцию-фокус**: Создаем обычную функцию на Python.
2.  **Вешаем "табличку" \`@tool\`**: Прямо над нашей функцией мы пишем декоратор \`@tool\`. Это как бы "заряжает" нашу функцию магией, превращая ее в инструмент, понятный джинну.
3.  **Пишем инструкцию**: В тройных кавычках \`"""Инструкция"""\` мы простым языком объясняем, **что делает наш фокус и когда его использовать**. Джинн будет читать эту инструкцию, чтобы понять, какой инструмент ему нужен для ответа на наш вопрос.

### Проект: Джинн-продавец телефонов
Создадим джинна, который "работает" в нашем магазине телефонов. У него будет два фокуса: узнать названия всех телефонов и найти телефон по имени.`,
{
  language: 'python',
  code: `# pip install langgraph
from langchain_gigachat import GigaChat
from langchain.tools import tool
from typing import Dict, List
from langgraph.prebuilt import create_react_agent
from langgraph.checkpoint.memory import MemorySaver

# --- Наша "база данных" телефонов (просто список словарей) ---
database = [
    {"name": "Pixel 8a", "price": 500, "memory": "128gb", "description": "Отличный телефон с лучшей камерой."},
    {"name": "Galaxy S24", "price": 1000, "memory": "256gb", "description": "Флагман от Samsung со стилусом."},
    {"name": "Redmi Note 13", "price": 250, "memory": "128gb", "description": "Лучший бюджетный телефон."}
]

# --- 1. Создаем фокусы (инструменты) ---

@tool
def get_all_phone_names() -> List[str]:
  """Используй этот инструмент, чтобы получить НАЗВАНИЯ ВСЕХ телефонов, которые есть в магазине."""
  print("--- [Джинн использовал инструмент 'get_all_phone_names'] ---")
  return [phone["name"] for phone in database]

@tool
def get_phone_by_name(name: str) -> Dict:
  """Используй этот инструмент, чтобы получить ПОЛНУЮ ИНФОРМАЦИЮ о телефоне по его точному имени."""
  print(f"--- [Джинн использовал инструмент 'get_phone_by_name' с именем: {name}] ---")
  for phone in database:
    if phone["name"].lower() == name.lower():
      return phone
  return {"error": "Телефон не найден"}

# --- 2. Собираем супер-джинна (Агента) ---
gigachat = GigaChat(credentials="ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_СЮДА", verify_ssl_certs=False)
tools = [get_all_phone_names, get_phone_by_name]

# MemorySaver() - это самая простая память, как записная книжка.
# Он будет помнить диалог, пока программа работает.
memory = MemorySaver() 

# create_react_agent - это заклинание, которое создает для нас умного джинна (агента),
# который умеет думать, выбирать инструмент, использовать его и на основе ответа формулировать финальный ответ.
agent_executor = create_react_agent(gigachat, tools=tools, checkpointer=memory)

# --- 3. Начинаем диалог ---
# У каждого диалога должен быть свой "id", как номер чата в мессенджере.
# Так джинн не перепутает наш разговор с разговором другого человека.
config = {"configurable": {"thread_id": "my-phone-dialog-1"}}

# Создадим маленькую функцию для удобства общения
def chat_with_agent(user_input):
    print(f"\\nВЫ: {user_input}")
    # Мы отправляем агенту не просто текст, а список сообщений
    response = agent_executor.invoke(
        {"messages": [("user", user_input)]},
        config=config
    )
    # Ответ агента тоже находится в списке сообщений, берем последнее
    print("ДЖИНН-ПРОДАВЕЦ:", response["messages"][-1].content)

chat_with_agent("Привет! Какие телефоны у вас есть в наличии?")
chat_with_agent("Расскажи-ка поподробнее про Galaxy S24. Сколько он стоит?")
chat_with_agent("А какой самый дешевый?") # Проверяем, помнит ли он контекст. Он должен снова вызвать get_phone_by_name для Redmi.
`
}
]
  },
  // LangGraph Workflow остается без изменений, так как он уже на довольно продвинутом уровне и хорошо объяснен через аналогию.
  // Добавление новых деталей может перегрузить учеников.
  {
    id: 'langgraph-workflow',
    title: 'LangGraph: Собираем ИИ-команду 👷‍♂️👷‍♀️',
    description: 'Собираем сложного агента "вручную" из нескольких узлов. Он сможет сам решать, когда искать информацию, а когда отвечать.',
    category: 'LangChain & GigaChat',
    tags: ['langgraph', 'workflow', 'планировщик', 'агент', 'шаблоны'],
    content: [
`Иногда нам нужно, чтобы над задачей работала целая команда из нескольких ИИ. LangGraph позволяет нам построить такую "команду" (ее называют "граф" или "рабочий процесс").

### Основная идея: Офис для ИИ
Представь, что мы строим офис для нашей ИИ-команды.
- **Узел (Node)**: это отдельный "работник" в нашем офисе. У каждого своя задача.
  - *Работник-Планировщик*: Это менеджер. Он получает задачу и решает, что делать дальше.
  - *Работник-Искатель*: Это стажер. Менеджер отправляет его в интернет за информацией.
  - *Работник-Писатель*: Это копирайтер. Он берет всю найденную информацию и пишет красивый финальный ответ.
- **Ребро (Edge)**: это "коридор", по которому работники передают друг другу документы. Иногда менеджер может построить "развилку": ЕСЛИ информации мало, отправить по одному коридору (к Искателю), ЕСЛИ достаточно — по другому (к Писателю).
- **Состояние (State)**: это общая папка на сервере, куда все работники складывают свои документы (вопрос, результаты поиска, финальный ответ), чтобы у всех был доступ.

### Заготовка: ИИ-исследователь
Эта команда сможет отвечать на вопросы, требующие поиска свежей информации в интернете.`,
{
  language: 'python',
  code: `import os
from typing import TypedDict, Literal, List
from langchain_community.tools.tavily_search import TavilySearchResults
from langchain_gigachat import GigaChat
from langgraph.graph import StateGraph, END
from langgraph.checkpoint.memory import MemorySaver
from pydantic import BaseModel, Field

# --- 1. Инструменты и Модель ---
os.environ["TAVILY_API_KEY"] = "ВАШ_КЛЮЧ_TAVILY" # Ключ для поискового сервиса Tavily
GIGACHAT_API_KEY = "ВАШ_КЛЮЧ_GIGACHAT"
search_tool = TavilySearchResults(max_results=2)
llm = GigaChat(credentials=GIGACHAT_API_KEY, verify_ssl_certs=False)

# --- 2. Определяем "общую папку" (Состояние) ---
class GraphState(TypedDict):
    question: str
    search_results: List[str]
    final_answer: str

# --- 3. Определяем "работников" (Узлы) ---

# Работник-Искатель: просто ищет в интернете
def search_node(state: GraphState):
    print("--- УЗЕЛ: РАБОТНИК-ИСКАТЕЛЬ ---")
    question = state["question"]
    results = search_tool.invoke({"query": question})
    # Кладем результат в "общую папку"
    return {"search_results": results}

# Работник-Писатель: пишет финальный ответ
def generate_node(state: GraphState):
    print("--- УЗЕЛ: РАБОТНИК-ПИСАТЕЛЬ ---")
    question = state["question"]
    search_results = state["search_results"]
    prompt = f"Дай развернутый ответ на вопрос '{question}', основываясь на этой информации из поиска: {search_results}"
    response = llm.invoke(prompt)
    # Кладем результат в "общую папку"
    return {"final_answer": response.content}

# --- 4. Сборка "офиса" (Графа) ---
workflow = StateGraph(GraphState)

# Добавляем работников в офис, давая им имена
workflow.add_node("searcher", search_node)
workflow.add_node("generator", generate_node)

# Указываем, кто начинает работать первым
workflow.set_entry_point("searcher")

# Прокладываем "коридоры" между работниками
workflow.add_edge("searcher", "generator") # После "searcher" всегда идет "generator"
workflow.add_edge("generator", END) # END значит "конец работы", офис закрывается

# --- 5. Компиляция и запуск ---
# .compile() собирает наш офис в готовую программу
app = workflow.compile()

# Запускаем нашу команду с вопросом
initial_state = {"question": "Кто выиграл последний Чемпионат мира по футболу и в каком году?"}
final_state = app.invoke(initial_state)

# Печатаем финальный ответ от Работника-Писателя
print("\\n--- ФИНАЛЬНЫЙ ОТВЕТ ---")
print(final_state["final_answer"])`
}
]
  },
];